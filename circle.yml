machine:
  ghc:
    version: 8.0.1
  services: {}

dependencies:
  cache_directories:
    - "~/.stack"
  pre:
    - wget -qO- https://get.haskellstack.org/ | sh
  override:
    - stack setup
    - stack build

test:
  override:
    - stack test